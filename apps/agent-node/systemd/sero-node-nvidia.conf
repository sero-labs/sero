[Service]
# NVIDIA tools need the host device namespace. Keep the device cgroup closed and
# allow only NVIDIA character-device groups registered in /proc/devices.
PrivateDevices=no
DevicePolicy=closed
DeviceAllow=char-nvidia* rw
