# Sero styleguide

Run `pnpm styleguide` from the repository root to inspect shared themes,
components and saved UX prototypes.

Files under `public/prototypes/` are design references. Add links to `src/PrototypeArchive.tsx` so each saved
prototype is available from the styleguide.
