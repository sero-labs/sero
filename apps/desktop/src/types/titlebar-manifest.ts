/** Title-bar contribution declared in an app manifest (`sero.app.titlebar`). */
export interface TitleBarManifest {
  /** Exported component name from the module federation remote. */
  component: string;
}
