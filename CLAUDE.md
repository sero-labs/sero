@AGENTS.md

## Agent skills

### Issue tracker

Issues live in the `sero-labs/sero` GitHub repo, managed with the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.