{
  "name": "@sero/app-runtime",
  "version": "0.1.0",
  "description": "Hooks for Sero federated app modules — useAppState, useAppInfo, useAgentPrompt",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "build:npm": "rm -rf dist/npm && tsc -p tsconfig.build.json && node scripts/prepare-npm.mjs",
    "typecheck": "tsc --noEmit"
  },
  "publishConfig": {
    "access": "public"
  },
  "peerDependencies": {
    "react": "catalog:peer"
  },
  "devDependencies": {
    "@types/react": "catalog:",
    "typescript": "catalog:"
  }
}
