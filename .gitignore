node_modules/
dist/
**/.DS_Store
*.log
.env
.env.local
.env.test
.vite/
.__mf__temp/
.turbo/
*.tgz

# Playwright
test-results/
playwright-report/
.playwright-cli/
apps/desktop/.sero-e2e/
apps/desktop/.sero-layout-test/
apps/desktop/.sero-test-data/
.sero-test-data/

# Local agent scratch data
.superpowers/
.pi/

apps/desktop/release/
apps/desktop/sero-desktop-*.tgz

.claude/

# Security scans: keep curated high/critical TXT summaries in git; ignore full Trivy JSON dumps
# which are large (multi-MB each) and easy to regenerate from the docker image with `trivy image`.
docs/security/scans/**/*-trivy-full.json

# e2e LLM credentials (per-developer, never committed)
apps/desktop/e2e/.env.test
