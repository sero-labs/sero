## 1. Prototype and explicit approval gate

- [x] 1.1 Use `sero-prototype` to build the project-menu entry, project tier controls and visual run inspector under `apps/styleguide/public/prototypes/architect-run-observability/`, with an archive entry. Base it on current Architect UI and the existing `sero-architect/` prototype. Verify that the styleguide route opens and that inherited, overridden and pending model selections are distinguishable.
- [x] 1.2 Make timeline expansion, time-range selection, filters, linked charts and selected-activity details interactive. Include initial delivery, maintenance, parallel work, independent review, repair, waits, compaction, failure and incomplete-history fixtures. Verify keyboard access, non-color status cues and wide/narrow desktop layouts with the checks prescribed by `sero-prototype`.
- [x] 1.3 Present the prototype and STOP for explicit user approval. Reconcile the proposal, design, specifications and remaining tasks with any approved changes. Verify completion with the user's recorded approval and a coherent updated artifact set; do not treat silence or a successful build as approval. No production task below may start before this gate passes.

## 2. Shared contracts and durable run ownership

- [x] 2.1 Add optional typed project model snapshots, configuration revisions, run correlation and explicit trigger intent to the existing common runtime/dispatch contracts. Keep ordinary non-Architect callers unchanged. Verify with type checks and focused dispatch tests covering absent context and rejected foreign-project attribution.
- [x] 2.2 Add backward-compatible project tier overrides and run references to Architect's runtime-owned record, separate from the hot index's detailed data. Verify loading old records, restoring inheritance, atomic updates and unchanged legacy budget amounts in record-store tests.
- [x] 2.3 Add versioned profile-local run/shared-activity journals, atomic summary checkpoints, source watermarks and paged readers. Verify replay idempotence, an interrupted final journal record, bounded read size, profile isolation and cleanup through the existing explicit project deletion path.
- [x] 2.4 Attach initial-delivery and maintenance-objective run identities before their first work; preserve them across milestones, retries, pause/resume, restart and late work after Stop. Verify lifecycle tests for multi-milestone maintenance, dismissed triage, duplicate causes, and continued in-flight work without false acceptance.
- [x] 2.5 Link coalesced owner activity and maintenance watcher overhead without guessing cost allocations. Verify that one shared call appears from both linked runs but is charged once in lifetime totals, and that legacy unassigned usage remains visible.

## 3. Project model resolution and safe changes

- [x] 3.1 Resolve effective tier entries using project overrides and global inheritance, preserving explicit worker pins and the existing owner environment pin with provenance. Validate supported model/thinking pairs. Verify focused resolution tests for clear/reset, per-project isolation, unavailable models and unsupported thinking without silent provider fallback.
- [x] 3.2 Save effective tier snapshots before Room/Workflow planning and pass them through planners, trigger extraction, workers, evaluators, recovery and revisions. Verify a fake host records the expected concrete model/thinking on every call class and that later steps/retries/recurring runs retain the dispatch snapshot after global changes.
- [x] 3.3 Apply current defaults to new direct research/capture calls and the next idle owner turn without changing active work. Use the existing owner regrant path when required, preserve earlier session references and charges, and re-prime current state. Verify active-turn stability, grant refusal, approved replacement and the single-owner-driver invariant.
- [x] 3.4 Implement project model management actions and connect the approved menu controls through the existing plugin action path. Update all affected shared, main-process, preload and renderer contracts together where that path requires them. Verify saved/inherited/effective values, explicit pin provenance and clear feedback about existing dispatches keeping earlier defaults.

## 4. Host and runtime observations

- [x] 4.1 Add optional metadata-only observation contracts with distinct operation, request, tool-call, turn, session and attempt identities, plus cache usage fields. Consult the installed Pi SDK documentation before mapping any new events. Verify the common types and adapter fixtures cover supported observations and explicit unknowns without exposing reasoning or raw payloads.
- [x] 4.2 Instrument subagent execution from queue admission and startup through model requests, tool calls, format repair and completion. Preserve cache counters and actual resolved model provenance. Verify parallel same-name tools, aborted setup, failed requests, repeated cumulative usage and telemetry failure using the existing runner/single-run test suites.
- [x] 4.3 Extend persistent-session observations for owner and Room turns with request/call identities and available timing, including compaction. Verify idle gaps are not active work, reopening retains source continuity, reasoning text stays excluded, and a new grant does not erase previous usage.
- [x] 4.4 Add semantic operation spans and correlation to Architect services and Orchestrator planning, Room, Workflow and auxiliary paths. Cover research, planning repair, trigger inference, evaluations, worker retries, command evidence, capture and delivery. Verify a deterministic full-run fixture contains every expected activity once with correct parents or links.
- [x] 4.5 Record observed approval, queue, pause and backoff intervals without inferring unknown time. Verify an approval wait overlaps a second worker's active interval correctly and a stopped owner does not close a still-running child operation.

## 5. Aggregation, historical coverage and a measured baseline

- [ ] 5.1 Extend existing accounting helpers so budget and trace summaries consume the same source deltas. Compute parent inclusive cost without rebilling children and preserve aggregate-only coverage. Verify partial/duplicate/reordered reports, failed attempts, shared activity, restart reconciliation and exact reconciliation with known project spend.
- [ ] 5.2 Implement timing unions, separate worker-time counters, token composition and configuration provenance summaries. Verify overlapping intervals count once as active run time, cache categories do not overlap after normalization, unsupported counters remain unknown and filtered totals have explicit scope.
- [ ] 5.3 Read legacy records into honest partial summaries without altering them or reconstructing fictional calls. Verify missing planning usage, old owner sessions, unpriced models and stale/pruned references with sanitized fixtures derived from observed record shapes.
- [ ] 5.4 Before changing efficiency guidance, record a bounded baseline for one implementation-and-independent-review objective and one collaborative-planning objective using the new metrics. Verify the record names the candidate, models, acceptance criteria, actual cost coverage and time; use synthetic data only for instrumentation checks, never as evidence of model efficiency. Keep any live-model budget bounded and record the evaluation in the linked GitHub issue or PR.

## 6. Delegation and context efficiency

- [ ] 6.1 Update owner and Room/Workflow planning guidance together: evaluable objectives, task-specific worker purposes, solution planning in appropriate Rooms, Workflow-owned execution planning, and no mandatory mode sequence or report-only worker. Verify existing planner/dispatch tests still preserve approvals, dynamic routing and completion; assess actual planning behavior in task 8.2 rather than relying on string assertions.
- [ ] 6.2 Replace repeated owner history with compact authoritative wake projections and reachable project-local context references. Reconstruct active context after compaction or regrant. Verify large-record size bounds, complete authority fields, current revisions, stale/missing references and owner access from its granted directory.
- [ ] 6.3 Pass relevant approved decisions/evidence into delegates; keep complete context accessible in their actual execution workspace, retain required inline input for tool-free model steps, and replace Room display-name relevance filtering with explicit/global applicability. Verify workspace and managed-worktree reads, no full-trace copying, preserved route variables and unnamed global decisions reaching affected members.
- [ ] 6.4 Keep independent evaluation separate from implementer self-checks and constrain repair/re-review to findings and affected behavior while preserving existing within-session repair. Verify missing functionality prevents acceptance despite passing development tests, a reviewer's product edit still needs independent judgment, and valid unrelated evidence is not invalidated merely by a new worker or milestone.
- [ ] 6.5 Skip trigger inference only for explicit one-off or fully supplied validated trigger intent; preserve normal execution planning and natural-language trigger extraction. Verify the fake host records no extractor call for a one-off milestone, correct supplied maintenance triggers, and unchanged extraction for an unspecified scheduled request.

## 7. Approved production run inspector

- [ ] 7.1 Add authorized summary/detail query and live-update actions through existing Architect app-runtime contracts, with bounded pagination and metadata-only responses by default. Synchronize any affected main/preload/renderer types. Verify foreign-project requests, raw secret-bearing fields and stale evidence links cannot leak content, and opening the main project page does not load trace detail.
- [ ] 7.2 Implement the approved full-width inspector and menu navigation with run/lifetime selection, expandable virtualized activity timeline, time-range zoom and activity/model/failure filters. Use shared state and the host layout service for persisted preferences. Verify keyboard navigation, return-to-project context, stable live selection and bounded rendering on a high-span fixture.
- [ ] 7.3 Connect cumulative spend, activity/model breakdown and token composition charts to timeline highlights and the selected-activity panel. Verify exact values, inclusive versus exclusive aggregation, shared versus attributable cost, actual model/thinking provenance and full-run versus filtered totals with deterministic UI tests.
- [ ] 7.4 Implement loading, empty, active, waiting, failed, interrupted and incomplete-history states from the approved prototype. Verify statuses are not color-only, unsupported timing is unavailable rather than zero, and Stop still shows late worker activity and usage.

## 8. Integration, outcome comparison and delivery

- [ ] 8.1 Run focused integrated checks across Architect, Orchestrator and host sessions for a dynamic Room-to-Workflow handoff, independent review/repair, model change during work, restart with partial telemetry, and retained budget/permission boundaries. Verify each affected delta-spec scenario has concrete test or observed evidence; reuse valid existing verification/resilience coverage rather than rerunning the full app trial suite.
- [ ] 8.2 Repeat the bounded task 5.4 evaluation after the efficiency changes with the same acceptance criteria and model settings. Compare attributable/shared cost, elapsed/active/wait time, requests, cache usage, retries and accepted outcomes. Verify the report separates measured improvements, regressions and unknowns, and does not equate fewer agents with success or claim savings from incomplete historical totals.
- [ ] 8.3 Update relevant Architect, Room and Workflow docs in `apps/docs-site/docs/`, subsystem guidance and any changed cross-cutting boundary in `ARCHITECTURE.md`. Verify docs match model inheritance, safe-boundary snapshots, run grouping, shared cost and menu-based inspection, with no claim of a hard budget ceiling or complete legacy traces. Note any common-package publication requirement.
- [ ] 8.4 Run the closest changed-area suites, root `pnpm typecheck`, React Doctor after React source changes, and the relevant styleguide/docs build checks. Verify the final diff contains no unintended source over 500 LOC, no browser storage, no unrelated workspace/toolchain changes, and that the approved prototype and requirements-to-evidence record match the delivered behavior.
