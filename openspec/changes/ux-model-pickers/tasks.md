## 1. The shared picker

- [ ] 1.1 Rebuild `AvailableModelPicker` (`packages/ui/src/components/model-selection/available-model-picker.tsx`) on `Combobox` from `packages/ui/src/components/ui/combobox.tsx`, keeping its current props and generics. Render one flat list: each row names the model and its provider, and the closed field names the chosen model with its provider beside it. Set a `filter` predicate on `Combobox.Root` that matches provider, model name and model id with the same rule `filterModelGroups` uses. Verify `pnpm --filter @sero-ai/ui test` passes a new `available-model-picker.test.tsx` covering filter by provider, by model name, by model id, the provider on a row, the provider in the closed field, and the keyboard choice.
- [ ] 1.2 Add an optional `leadingOptions: ReadonlyArray<{ value: string; label: string }>` prop. List those entries before the models, filter them with the same query, and resolve the closed field against them before the catalogue. Verify the test covers a chosen leading option shown by its label and filtered together with the models.
- [ ] 1.3 Keep a saved value the catalogue cannot resolve visible. Verify the test covers a saved model that is not in the `groups` list: the closed field shows the saved value and the list still offers the available models.
- [ ] 1.4 Keep `allowClear` clearing in one action. Verify the test covers the clear control removing the value and showing the placeholder.
- [ ] 1.5 Confirm `ModelPickerBody`, `ProviderLogo` and `thinking-picker.tsx` are unchanged, so the remote composer keeps its grouped list. Verify `pnpm --filter @sero-ai/ui typecheck` and the existing `model-picker-body.test.tsx` pass.

## 2. A step's model is one field

- [ ] 2.1 Change `StepModelControl` (`plugins/sero-orchestrator-plugin/ui/components/StepModelControl.tsx`) to one `AvailableModelPicker` with `leadingOptions` of `Auto`, `LOW`, `MED`, `HIGH`, and `allowClear`. Map `Auto` and an empty value to `onChange(undefined, undefined)`, a tier to `onChange(tier, undefined)` and a model key to `onChange(key, undefined)`. Delete the `customArmed` state, the `CUSTOM` sentinel and the second picker. Give the model field the width the drawing shows (frame 1 is about ten times a single model name, not the current `min-w-[12rem]`). Verify a new `step-model-control.test.tsx` covers the Auto default, a tier choice, a model pin whose field names the provider, and clearing the pin back to Auto.

## 3. Architect's model fields

- [ ] 3.1 Change the tier model field in `ModelSettings.tsx` (`plugins/sero-architect-plugin/ui/components/ModelSettings.tsx`) from the styled `Select` to `AvailableModelPicker`, with one `leadingOptions` entry holding the existing `NONE` value, labelled `Global` when the inherited selection cannot be read and `Not selected` otherwise. In `onChange`, when the value is `NONE`, call `actions.clearModelDefault`; otherwise resolve the model option and call `setModelDefault`. The current handler returns early for a value it cannot find, so the first choice would be inert without this branch. Keep the thinking `Select` beside it and keep the `Use global` button. Add a picker stand-in beside `select-stand-in.tsx` and use it in `model-settings.test.tsx` and in `project-controls.test.tsx`, which also mocks `@sero-ai/ui` and renders `IntakeDialog` (and so `IntakeModelOverrides`). Verify the tests cover an override, restoring inheritance by picking the first choice, and the unreadable-global label.
- [ ] 3.2 Change the Effective cell in `ModelSettings.tsx` to `<model id> · <thinking level>` from the tier's effective entry, in place of the provider-qualified reference it prints today, matching frame 2. Leave the owner row's Effective cell as it is; the drawing shows no owner row. Update the existing assertion in `model-settings.test.tsx` that says the Effective column names the model only, and add a tier-row assertion for the model id and the thinking level. Verify `model-settings.test.tsx` passes.
- [ ] 3.3 Change `IntakeModelOverrides.tsx` (`plugins/sero-architect-plugin/ui/components/IntakeModelOverrides.tsx`) to the same picker, with `Global selection` as the leading option and the existing `GLOBAL` sentinel. Keep the thinking `Select` and the folded row. Verify a test covers setting a tier, clearing it back to `Global selection`, and the thinking level staying beside the field.
- [ ] 3.4 Confirm the saved values and actions did not change: the field still writes `provider/modelId` for a model, the tier for an override, and clears with the caller's sentinel. Verify the existing model-resolution and Architect action suites pass with no edit to `shared/model-config` or `shared/record`.

## 4. One picker on every Architect and Orchestrator screen

- [ ] 4.1 Inventory every model picker in both plugins and confirm each uses the shared field. Verify the only model-choice sites are `ModelSettings.tsx`, `IntakeModelOverrides.tsx` and `StepModelControl.tsx`, that each imports `AvailableModelPicker`, and that no native `<select>` or styled `Select` chooses a model in either plugin. Record the inventory in `comparison.md`.
- [ ] 4.2 Confirm the read-only model displays are not pickers and keep their format: the Room member facts, the Room's allowed-models chips, `ModelChoices.tsx`, and the inspector. Verify no change is made to them and no model choice is reachable from them.

## 5. Evidence

- [ ] 5.1 Add an Orchestrator preview that renders the real `StepCard` for a step with Tune open, beside its agent and tools controls, and register it in `plugins/sero-orchestrator-plugin/ui/__preview__/previews.tsx`. Give the preview a model catalogue with several providers and enough models that rows, provider labels, filtering and the chosen check mark are all visible. Verify `pnpm --filter @sero-ai/plugin-orchestrator preview` serves it at `ui/__preview__/index.html?preview=<id>` and the Tune panel shows the model field with its list.
- [ ] 5.2 Extend the Architect preview fixture (`plugins/sero-architect-plugin/ui/__preview__/main.tsx`) with several providers and models, so the open list at `?state=models` shows rows, providers and a chosen check mark, matching frame 2. Verify `pnpm --filter @sero-ai/plugin-architect preview` renders the open list.
- [ ] 5.3 Capture the drawing's two frames from `8-model-pickers.html` and the built surfaces (Orchestrator step Tune via the new preview; Architect `?state=models` via `pnpm --filter @sero-ai/plugin-architect preview`) at a viewport wider than the panel, opening every fold, chevron and Tune panel on both sides. The drawing side was already rendered and read at planning time; see `comparison.md` for the method and what each frame shows. Verify each capture was read as an image, side by side, before any difference was recorded, and record the method in `comparison.md`.
- [ ] 5.4 Write `openspec/changes/ux-model-pickers/comparison.md` with the capture method, the model-picker inventory, each difference the captures showed and what was done about it, the deliberate departures (the flat list with no provider logos; the Project models first-choice label; the owner row's Effective cell; the three uncaptured shared-picker consumers), and the frames that were never compared.
- [ ] 5.5 Confirm every captured difference is closed or recorded. Verify no difference in `comparison.md` is left open, and any behaviour the drawing drops is still reachable and named in the departures.

## 6. Checks

- [ ] 6.1 Run `pnpm typecheck` from the monorepo root and confirm no error in the renderer or the Electron main process. Confirm the four unchanged consumers (Admin `AgentEditor.tsx` and `ModelPanel.tsx`, onboarding `SetupScreen.tsx`, Design Library `SettingsPage.tsx`) compile against the unchanged props.
- [ ] 6.2 Run the touched suites - `pnpm --filter @sero-ai/ui test`, the Architect `ui/__tests__` suites and the Orchestrator `ui/__tests__` suites - and record the result before review.
- [ ] 6.3 Bump the `@sero-ai/ui` version in `packages/ui/package.json` in the same pull request, because it is published to npm. Verify `pnpm --filter @sero-ai/ui build` passes.
- [ ] 6.4 Open one draft pull request that references issue #543, with the change summary and the `comparison.md` captures, and confirm the issue link is in the description.
