diff --git a/lib/index.js b/lib/index.js
index 7e912d0..3978a75 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -5410 +5410 @@ function resolveProxyAlias(binding, proxyLocal, code, fullImport, claimedLocals = /* @__PURE__ */ new Set()) {
-	const localUsedInCode = new RegExp(`\\b${escapedLocal}\\b`).test(codeWithoutImport);
+  const localUsedInCode = new RegExp(`(?<![$\\w])${escapedLocal}(?![$\\w])`).test(codeWithoutImport);
