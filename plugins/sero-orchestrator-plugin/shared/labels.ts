/**
 * User-facing mode names (ux-refit-plan.md D3). "Loops" is renamed
 * "Workflows" in labels and notifications only — code identifiers, files,
 * state keys and tool params all stay `loop`.
 */
export const WORKFLOW_LABEL = 'Workflow';
export const WORKFLOWS_LABEL = 'Workflows';
