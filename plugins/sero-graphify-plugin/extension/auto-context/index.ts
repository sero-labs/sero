import type { ExtensionAPI } from '@earendil-works/pi-coding-agent';
import type { GraphifyPaths } from '../../shared/paths';

// Temporary no-op — replaced with the real auto-context port in Task 15.
export function registerAutoContext(_pi: ExtensionAPI, _paths: GraphifyPaths): void {}
