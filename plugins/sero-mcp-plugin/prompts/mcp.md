---
description: Use the MCP tool for status, discovery, resource reads, and tool calls
---
Use the \`mcp\` command/tool for this request: $@

Do not start with \`mcp_manager\` unless this is explicitly MCP setup, auth, diagnostics, or viewer work.
If the exact server, tool name, or arguments are unclear, inspect with \`mcp tools <server>\` or \`mcp describe <server> <tool>\` first.
For CLI usage, the short forms \`tools/resources/describe/call/read\` and action-style aliases like \`list_tools/read_resource\` are both accepted.
If no request text was supplied, show MCP status with \`mcp\`.
